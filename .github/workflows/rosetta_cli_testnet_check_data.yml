name: rosetta_cli_testnet_check_data

on:
  push:
    branches:
      - dev

jobs:
  rosetta_cli_data_construction:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: install deps
        run: |
          sudo apt-get install sed jq psmisc

      - name: build
        run: |
          cargo build

      - name: run rosetta_cli_testnet.sh
        run: INSTALL=1 DATA=1="rosetta-ci" ./rosetta_cli_testnet.sh